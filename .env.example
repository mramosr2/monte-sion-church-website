# ---- Contact form email (server only) ----
# Use any SMTP provider (Google Workspace, SendGrid SMTP, Mailgun SMTP, etc.)

# Where your SMTP server lives
SMTP_HOST=smtp.example.com
SMTP_PORT=587
# Set true for port 465, otherwise false
SMTP_SECURE=false

# SMTP credentials (if required by your provider)
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Where messages get delivered (TESTING)
MAIL_TO=MisionMonteSionLA@gmail.com

# ---- Client (Vite) ----
# For GitHub Pages / static hosting: set this to your Cloudflare Worker endpoint.
# Example: https://monte-sion-contact.your-subdomain.workers.dev/api/contact
VITE_CONTACT_ENDPOINT=

# The From header used by the website (often must be a verified sender for your SMTP provider)
MAIL_FROM="Monte Sion Website <no-reply@example.com>"

# Optional subject prefix
MAIL_SUBJECT_PREFIX="[Monte Sion Website]"

# ---- Spam/rate limiting ----
# Each IP can submit up to RATE_LIMIT_MAX messages in RATE_LIMIT_WINDOW_MINUTES.
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX=5

# Server port for local dev
PORT=8787
